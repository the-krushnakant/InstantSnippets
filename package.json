{
  "name": "instant-snippets",
  "displayName": "Instant Snippets",
  "description": "Quickly open and run Python snippets from selected text",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "instantSnippets.openInEditor",
        "title": "Open snippet in editor"
      },
      {
        "command": "instantSnippets.openAndRun",
        "title": "Open in editor and run snippet"
      },
      {
        "command": "instantSnippets.openImportAndRun",
        "title": "Open in editor, add 'import os' and run snippet"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "when": "editorHasSelection && editorLangId == python",
          "command": "instantSnippets.openInEditor",
          "group": "instantSnippets@1"
        },
        {
          "when": "editorHasSelection && editorLangId == python",
          "command": "instantSnippets.openAndRun",
          "group": "instantSnippets@2"
        },
        {
          "when": "editorHasSelection && editorLangId == python",
          "command": "instantSnippets.openImportAndRun",
          "group": "instantSnippets@3"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.60.0",
    "@types/node": "^14.14.37",
    "@typescript-eslint/eslint-plugin": "^4.22.0",
    "@typescript-eslint/parser": "^4.22.0",
    "eslint": "^7.24.0",
    "typescript": "^4.2.4"
  }
}